# Compatibility Matrix

本文件记录 `mac-folder-icon-replacer` 在不同 Agent、macOS 环境和权限状态下的真实验证结果。

## 判定状态

- **已验证**：当前 Skill 版本、宿主、Mac 和原生工具构建完成全部隔离端到端验收项，并保留 JSON 报告。
- **部分验证**：可以加载 Skill，但某一项必要能力需要人工完成或无法自动完成。
- **未验证**：尚未在目标 Agent 上完成真实测试。
- **不兼容**：无法加载 Skill，或缺少完成安全流程所需的关键能力。

## 统一验收项

每个 Agent 都必须按同一测试夹具执行以下项目：

1. 加载通用 `SKILL.md`。
2. 运行无副作用安装 preflight，检查图像处理、原生工具和文件系统能力。
3. 未获授权时停止，不操作目标文件夹。
4. 使用 `tests/skill-test-image.jpg`。
5. 生成透明背景 PNG。
6. 展示 preflight 报告，并在隔离 E2E 前请求授权。
7. 复制测试目标夹具到临时目录，通过 `NSWorkspace.setIcon` 设置临时副本图标；Finder/剪贴板仅作降级测试。
8. 验证图标已更新。
9. 验证普通文件、嵌套目录和文件内容未改变。
10. 单独记录允许的 `Icon` 和 `.DS_Store` 系统元数据。
11. 生成测试结果和兼容性结论。

## 当前矩阵

| Agent | Skill 加载 | 图像处理 | Finder/剪贴板 | 内容保护验证 | 结论 | 证据 |
|---|---|---|---|---|---|---|
| Codex | 已验证 | 历史验证，待 1.1.0 preflight | 历史验证，待 1.1.0 隔离 E2E | 历史验证，待 1.1.0 隔离 E2E | 待重新验证 | 项目历史记录不自动继承到新版本 |
| WorkBuddy | 官方支持，待本机实测 | 待测 | 待测 | 待测 | 未验证 | `workbuddy.md`；官方 Skills 文档 |

## 约束

- 没有真实测试证据时，不得将 Agent 标记为“已验证”。
- 不因为 Agent 能读取 Markdown 就推断它能控制 Finder。
- 不因为图像能被处理就推断剪贴板和 Finder 权限可用。
- 任何普通文件或子目录发生非预期变化，整体测试失败。
- macOS/Finder 为显示自定义图标生成的系统元数据应单独记录，不计入普通内容变更。
